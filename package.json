{
  "name": "intento04",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open",
    "reportTest": "cypress run --spec cypress/features/test.feature --headless",
    "cypress:execution": "cypres run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "cypress": "^13.6.4",
    "esbuild": "^0.20.0",
    "multiple-cucumber-html-reporter": "^3.6.1"
  },
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^20.0.1"
  }
}
